{
  "dataset_name": "grounding_core_v1",
  "version": "1.1",
  "description": "Deterministic grounding/citation regression dataset -- concept-traceability-to-evidence and citation resolution/support/count checks. 1 expected_outcome=pass case plus 5 negative controls, each pinned to the stage it must fail at (2 retrieval-stage, 3 generation-stage), so a control that starts failing for the wrong reason is caught rather than silently counted as still working."
}
